version: '3.8'

services:
  chtime:
    # For local development - builds from source
    build:
      context: .
      dockerfile: Dockerfile
    # Alternative: Use pre-built image
    # image: ghcr.io/crypticbae/chtime:latest
    container_name: chtime-app
    ports:
      - "7777:3000"
    environment:
      - NODE_ENV=development
      - NEXT_TELEMETRY_DISABLED=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: Add a reverse proxy (uncomment if needed)
  # nginx:
  #   image: nginx:alpine
  #   container_name: chtime-nginx
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - ./ssl:/etc/nginx/ssl:ro
  #   depends_on:
  #     - chtime
  #   restart: unless-stopped 